# Cloudflare Pages (production / preview builds). Omit locally — Vite proxies `/api` to wrangler dev.
# Full Worker origin, no trailing slash.
# VITE_API_URL=https://andriveau-bobine-api.<your-subdomain>.workers.dev
